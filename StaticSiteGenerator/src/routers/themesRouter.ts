import express from "express";

export const themeRouter = express.Router();

themeRouter.get('/', async (_, res) => {
    throw new Error("Not Implemented");
});

themeRouter.get('/:id', async (_, res) => {
    throw new Error("Not Implemented");
});


themeRouter.get('/:id/:name', async (_, res) => {
    throw new Error("Not Implemented");
});

themeRouter.post('/:id/:name', async (_, res) => {
    throw new Error("Not Implemented");
});


themeRouter.put('/:id/:name', async (_, res) => {
    throw new Error("Not Implemented");
});


themeRouter.delete('/:id/:name', async (_, res) => {
    throw new Error("Not Implemented");
});

