import { HtmlLoadedDirective } from './html-loaded.directive';

describe('HtmlLoadedDirective', () => {
  it('should create an instance', () => {
    const directive = new HtmlLoadedDirective();
    expect(directive).toBeTruthy();
  });
});
